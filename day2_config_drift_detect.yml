# playbooks/d2_drift_detect.yml
---
# =========================================================================
# Play 1: Scan all hosts — baseline comparison + AIDE integrity check
# =========================================================================
- name: "Day 2 -- Drift Detection Scan"
  hosts: "{{ target_hosts | default('all') }}"
  become: true
  serial: "{{ detect_serial | default('20%') }}"
  gather_facts: true
  vars:
    drift_auto_remediate: >-
      {{ hostvars[inventory_hostname].drift_auto_remediate
         | default(environment != 'prod') }}

  tasks:
    # --- Baseline comparison (packages, services, sysctl, SSH, PAM, etc.) ---
    - name: Run baseline comparison checks
      ansible.builtin.include_role:
        name: qbe.rhel.baseline_check


    # --- Map drifted categories to Day 1 remediation roles ---
    # Mappings loaded from group_vars/all/rhel10_remediation.yml:
    #   category_role_map  — baseline category → Day 1 role
    #   agent_role_map     — agent name → Day 1 install role

    - name: Identify drifted categories
      ansible.builtin.set_fact:
        _drifted_categories: >-
          {{ drift_results.categories | dict2items
             | selectattr('value.status', 'equalto', 'drifted')
             | map(attribute='key') | list }}

    - name: Map drifted categories to Day 1 roles
      ansible.builtin.set_fact:
        _drifted_roles_from_categories: >-
          {{ _drifted_categories
             | reject('equalto', 'agents')
             | map('extract', category_role_map)
             | select('string')
             | unique | list }}

    - name: Map drifted agents to Day 1 roles
      ansible.builtin.set_fact:
        _drifted_roles_from_agents: >-
          {{ (drift_results.categories.agents.drift_items | default([]))
             | map(attribute='item')
             | map('extract', agent_role_map)
             | select('defined')
             | unique | list }}
      when: "'agents' in _drifted_categories"

    - name: Build role_drift list
      ansible.builtin.set_fact:
        _role_drift: >-
          {{ (_drifted_roles_from_categories + (_drifted_roles_from_agents | default([])))
             | unique
             | map('community.general.dict_kw', role='__item__', changed=true)
             | list }}

    # --- Consolidate per-host results ---
    - name: Build consolidated per-host drift fact
      ansible.builtin.set_fact:
        host_drift_summary:
          hostname: "{{ inventory_hostname }}"
          region: "{{ region }}"
          environment: "{{ environment }}"
          platform: "{{ platform | default('unknown') }}"
          scan_timestamp: "{{ ansible_date_time.iso8601 }}"
          auto_remediate: "{{ drift_auto_remediate }}"
          baseline_drift: "{{ drift_results }}"
          role_drift: "{{ _role_drift }}"
          aide_drift: "{{ aide_result is changed }}"
          aide_severity: "{{ (aide_result is changed) | ternary('major', 'none') }}"
          has_drift: >-
            {{ drift_results.has_drift or aide_result is changed }}

# # =========================================================================
# # Play 2: Consolidate results — generate report, create ticket, send email
# # =========================================================================
# - name: "Day 2 -- Drift Detection Consolidation"
#   hosts: localhost
#   gather_facts: true
#   connection: local
#   vars:
#     scan_targets: "{{ groups[target_hosts | default('rhel10_all')] }}"
#     all_results: >-
#       {{ scan_targets | map('extract', hostvars, 'host_drift_summary') | list }}
#     drifted_hosts: >-
#       {{ all_results | selectattr('has_drift', 'equalto', true) | list }}
#     clean_hosts: >-
#       {{ all_results | rejectattr('has_drift', 'equalto', true) | list }}
#     total_scanned: "{{ scan_targets | length }}"
#     total_drifted: "{{ drifted_hosts | length }}"
#     total_clean: "{{ clean_hosts | length }}"
#     drift_percentage: >-
#       {{ ((total_drifted | int / total_scanned | int) * 100) | round(1) }}
#     # Per-environment breakdown
#     env_summary: >-
#       {{
#         all_results | groupby('environment') | items2dict(
#           key_name=0, value_name=1
#         )
#       }}
#     report_filename: "drift-report-{{ ansible_date_time.date }}.html"
#     report_path: "/tmp/{{ report_filename }}"

#   tasks:
#     - name: Generate HTML drift report
#       ansible.builtin.template:
#         src: "{{ playbook_dir }}/../templates/drift_report.html.j2"
#         dest: "{{ report_path }}"
#         mode: "0644"
#       vars:
#         report_data:
#           scan_date: "{{ ansible_date_time.iso8601 }}"
#           total_scanned: "{{ total_scanned }}"
#           total_drifted: "{{ total_drifted }}"
#           total_clean: "{{ total_clean }}"
#           drift_percentage: "{{ drift_percentage }}"
#           drifted_hosts: "{{ drifted_hosts }}"
#           clean_hosts: "{{ clean_hosts }}"
#           env_summary: "{{ env_summary }}"

#     - name: Copy report to Satellite web server
#       ansible.builtin.copy:
#         src: "{{ report_path }}"
#         dest: "/var/www/html/reports/drift/{{ report_filename }}"
#         mode: "0644"
#       delegate_to: "{{ satellite_servers[region][platform] }}"
#       become: true

#     - name: Create consolidated ServiceNow ticket for drift
#       when: total_drifted | int > 0
#       servicenow.itsm.change_request:
#         state: new
#         type: normal
#         short_description: >-
#           Configuration drift -- {{ total_drifted }}/{{ total_scanned }}
#           hosts ({{ drift_percentage }}%) -- {{ ansible_date_time.date }}
#         description: |
#           Configuration Drift Detection Report
#           ======================================
#           Scan Date: {{ ansible_date_time.iso8601 }}
#           Total Hosts Scanned: {{ total_scanned }}
#           Hosts with Drift: {{ total_drifted }} ({{ drift_percentage }}%)
#           Hosts Clean: {{ total_clean }}

#           HTML Report: https://{{ satellite_servers[region][platform] }}/reports/drift/{{ report_filename }}

#           Drifted Hosts:
#           {% for host in drifted_hosts %}
#           --------------------------------------------------
#           Host: {{ host.hostname }} ({{ host.region }}/{{ host.environment }}/{{ host.platform }})
#           Drift items: {{ host.baseline_drift.total_drift }}
#           Max severity: {{ host.baseline_drift.max_severity | upper }}
#           Categories: {{ host.baseline_drift.categories | dict2items | selectattr('value.status', 'equalto', 'drifted') | map(attribute='key') | join(', ') }}
#           AIDE: {{ host.aide_drift | ternary('ALERT', 'clean') }}
#           {% endfor %}

#           {% if drifted_hosts | selectattr('auto_remediate', 'equalto', true) | list | length > 0 %}
#           Auto-remediation was applied to {{ drifted_hosts | selectattr('auto_remediate', 'equalto', true) | list | length }} non-prod host(s).
#           {% endif %}
#           Production hosts require manual remediation via change management.
#         risk: "{{ (drifted_hosts | selectattr('aide_drift', 'equalto', true) | list | length > 0) | ternary('high', 'moderate') }}"
#         impact: medium
#         assignment_group: "Linux-Operations"
#         category: Configuration Management
#       register: snow_drift_ticket
#       no_log: true

#     - name: Create security incident for major/critical drift
#       when: >-
#         drifted_hosts | selectattr('aide_drift', 'equalto', true) | list | length > 0 or
#         drifted_hosts | selectattr('baseline_drift.max_severity', 'in', ['major', 'critical']) | list | length > 0
#       servicenow.itsm.incident:
#         state: new
#         impact: high
#         urgency: high
#         short_description: >-
#           SECURITY: Major/critical configuration drift detected --
#           {{ ansible_date_time.date }}
#         description: |
#           Major/critical configuration drift detected on RHEL 10 systems.
#           Immediate investigation required.

#           HTML Report: https://{{ satellite_servers[region][platform] }}/reports/drift/{{ report_filename }}

#           {% for host in drifted_hosts | selectattr('baseline_drift.max_severity', 'in', ['major', 'critical']) | list %}
#           Host: {{ host.hostname }} ({{ host.region }}/{{ host.environment }})
#           Max severity: {{ host.baseline_drift.max_severity | upper }}
#           {% if host.aide_drift %}AIDE: File integrity changes detected{% endif %}
#           {% endfor %}
#         assignment_group: "Security-Operations"
#         category: Security
#         subcategory: "Configuration Drift"
#       register: snow_security_incident
#       no_log: true

#     - name: Send drift report email
#       community.general.mail:
#         host: "{{ smtp_server }}"
#         port: "{{ smtp_port }}"
#         from: "rhel10-drift@qbe.com"
#         to: "{{ ops_email.values() | flatten | unique | list }}"
#         subject: >-
#           RHEL 10 Drift Report -- {{ total_drifted }}/{{ total_scanned }}
#           hosts ({{ drift_percentage }}%) -- {{ ansible_date_time.date }}
#         subtype: html
#         body: "{{ lookup('file', report_path) }}"
#         attach:
#           - "{{ report_path }}"
#       when: total_drifted | int > 0

#     - name: Send clean summary email
#       community.general.mail:
#         host: "{{ smtp_server }}"
#         port: "{{ smtp_port }}"
#         from: "rhel10-drift@qbe.com"
#         to: "{{ ops_email.values() | flatten | unique | list }}"
#         subject: >-
#           RHEL 10 Drift Report -- ALL CLEAN --
#           {{ total_scanned }} hosts -- {{ ansible_date_time.date }}
#         body: |
#           All {{ total_scanned }} RHEL 10 hosts are compliant with baseline.
#           No drift detected. No action required.

#           Scan completed: {{ ansible_date_time.iso8601 }}
#       when: total_drifted | int == 0

#     - name: Store drift summary as workflow artifact
#       ansible.builtin.set_stats:
#         data:
#           drift_scan_date: "{{ ansible_date_time.iso8601 }}"
#           drift_total_scanned: "{{ total_scanned }}"
#           drift_total_drifted: "{{ total_drifted }}"
#           drift_percentage: "{{ drift_percentage }}"
#           drift_report_url: "https://{{ satellite_servers[region][platform] }}/reports/drift/{{ report_filename }}"
#           drift_snow_ticket: "{{ snow_drift_ticket.record.number | default('N/A') }}"

#     # --- Drift trending: persist scan summary as JSON for historical analysis ---
#     - name: Persist drift scan summary for trending
#       ansible.builtin.copy:
#         content: "{{ _trend_entry | to_nice_json }}"
#         dest: "/var/www/html/reports/drift/history/{{ ansible_date_time.date }}.json"
#         mode: "0644"
#       delegate_to: "{{ satellite_servers[region][platform] }}"
#       become: true
#       vars:
#         _trend_entry:
#           scan_date: "{{ ansible_date_time.iso8601 }}"
#           total_scanned: "{{ total_scanned | int }}"
#           total_drifted: "{{ total_drifted | int }}"
#           total_clean: "{{ total_clean | int }}"
#           drift_percentage: "{{ drift_percentage | float }}"
#           by_environment: >-
#             {{ all_results | groupby('environment')
#                | map('last')
#                | map('selectattr', 'has_drift', 'equalto', true)
#                | map('list') | map('length') | list }}
#           by_category: >-
#             {{ drifted_hosts
#                | map(attribute='baseline_drift.categories')
#                | map('dict2items')
#                | flatten
#                | selectattr('value.status', 'equalto', 'drifted')
#                | map(attribute='key')
#                | list | community.general.counter }}
#           by_severity: >-
#             {{ drifted_hosts
#                | map(attribute='baseline_drift.max_severity')
#                | list | community.general.counter }}
#           aide_alerts: "{{ drifted_hosts | selectattr('aide_drift', 'equalto', true) | list | length }}"

#     - name: Load last 12 weeks of drift history for trending
#       ansible.builtin.find:
#         paths: "/var/www/html/reports/drift/history/"
#         patterns: "*.json"
#         age: "-84d"
#       delegate_to: "{{ satellite_servers[region][platform] }}"
#       register: _trend_files

#     - name: Read historical drift summaries
#       ansible.builtin.slurp:
#         src: "{{ item.path }}"
#       loop: "{{ _trend_files.files | sort(attribute='path') }}"
#       loop_control:
#         label: "{{ item.path | basename }}"
#       delegate_to: "{{ satellite_servers[region][platform] }}"
#       register: _trend_data

#     - name: Build trend data for report
#       ansible.builtin.set_fact:
#         drift_trend: >-
#           {{ _trend_data.results
#              | map(attribute='content')
#              | map('b64decode')
#              | map('from_json')
#              | list }}
